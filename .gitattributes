* text=auto

# Shell scripts must keep LF. Checked out with CRLF, Linux rejects them with
# "bad interpreter: /usr/bin/env bash^M: no such file or directory".
*.sh   text eol=lf
Dockerfile text eol=lf
*.yml  text eol=lf
*.conf text eol=lf
